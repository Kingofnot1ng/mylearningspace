静态成员函数以 static 修饰，例如
	public static void hello(){
		cout<<"Hello world!"<<endl;	
	}
所有由同一个类实例化的对象共享同一个静态成员函数
静态成员也能用权限关键字修饰，其可以拥有不同的访问权限（public protected private）
可以不实例化对象，直接用 类名::静态成员函数名() 实现访问；
静态变量由static修饰，所有对象共享这一份变量，同样能用 类名::静态成员变量名 访问
静态成员函数只能访问静态变量。
