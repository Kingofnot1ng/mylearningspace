#include <iostream>
#include <list>

using namespace std;

int main(){
	//push_back(elem);
	//pop_back();
	//push_front(elem);
	//pop_front();
	//insert(pos,elem);
	//insert(pos,n,elem);
	//insert(pos,beg,end);
	//clear();
	//erase(bef,end);
	//erase(pos);
	//remove(elem);
	return 0;
}
