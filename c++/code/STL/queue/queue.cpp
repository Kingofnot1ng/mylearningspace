#include <iostream>
#include <queue>

using namespace std;


int main(){
	/*
		构造函数
		queue<T> que;
		queue<T> que(const queue& q); 
	*/
	
	/*
		赋值操作
		queue& operator=(const queue &q) 
	*/
	
	/*
		数据存取 
		push(elem);
		pop(); 
		back();
		font();
	*/
	
	/*
		大小 
		empty(); 
		size();
	*/
	return 0;
}
