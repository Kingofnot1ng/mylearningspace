#include <iostream>
#include <deque>
using namespace std;

int main(){
	//push_back(elem)
	//push_front(elem)
	//pop_back();
	//pop_font();
	
	//insert(pos,elem)
	//insert(pos.elem)
	//insert(pos,beg,end)
	//clear()
	//erase(pos)
	//erase(beg,end)
	return 0;
}
