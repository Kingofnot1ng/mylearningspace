#include <iostream>

using namespace std;

int main(){
	//push_back(elem)
	//pop_back()
	//insert(const_iterator pos,elem)
	//insert(const_iterator pos,int count,elem)
	//erase(const_iterator pos)
	//erase(const_iterator pos,const_iterator end)
	//clear()
	return 0;
}
